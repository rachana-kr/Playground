#include<iostream>
using namespace std;
int main()
{
  int r,l,d;
  std::cin>>r>>l;
  d=2*r;
  if(l==d)
    std::cout<<"circle can be inside a square";
  else 
    std::cout<<"circle cannot be inside a square";
}
  
  
